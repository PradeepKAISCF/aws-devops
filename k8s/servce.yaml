apiVersion: v1
kind: Service
metadata:
  name: gitops-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: gitops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30090 # Access app at PORT 30090
